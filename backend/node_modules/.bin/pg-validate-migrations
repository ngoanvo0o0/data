#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../postgres-migrations/dist/bin/validate.js" "$@"
  ret=$?
else 
  node  "$basedir/../postgres-migrations/dist/bin/validate.js" "$@"
  ret=$?
fi
exit $ret
